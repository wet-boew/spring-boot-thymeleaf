@Library(["ised-cicd-lib@CLOUDCICD-513", "ised-cicd-lib-api"]) _

pipeline {
	agent {
       	label 'maven'
   	}
   	
    options {
        disableConcurrentBuilds()
    }
    
    stages {
		stage('release') {
			steps {
				script{
					release.releaseMavenLibrary()
				}
			}
		}
    }
}
